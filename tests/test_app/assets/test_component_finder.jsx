var React = require('react'):

var static_file_finder_component = React.createClass({
    render: function() {
        return <span>You found me</span>
    }
})

module.exports = static_file_finder_component;
