import React from 'react';

class NavVar extends React.component {
    render():
        return (
            <div>
                <ul>{props.items}</ul>
            </div>
      );
}

export default NavVar
